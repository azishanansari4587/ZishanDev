/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['aceternity.com', 'assets.aceternity.com', 'images.unsplash.com'], // Add any other domains you may need
    },
};

export default nextConfig;
